name: Docker Image CI

on: [push]

jobs:

   build:

     runs-on: ubuntu-latest

     steps:
     - uses: actions/checkout@v1
     - name: Build the Docker image
       run: docker build . --file Dockerfile --tag docker.pkg.github.com/***REMOVED***/golinks/golinks:$(date +%s)
       run: docker login docker.pkg.github.com -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
       run: docker push docker.pkg.github.com/***REMOVED***/golinks/golinks:$(date +%s)
